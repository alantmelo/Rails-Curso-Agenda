<p id="notice"><%= notice %></p>

<h1><%= " #{t('listar')} #{@contacts.model.model_name.human(count: @contacts.count)}"%></h1>

<table>
  <thead>
    <tr>
      <th><%=@contacts.model.human_attribute_name(:name)%> </th>
      <th><%=@contacts.model.human_attribute_name(:email)%></th>
      <th><%=@contacts.model.human_attribute_name(:kind)%></th>
      <th><%=@contacts.model.human_attribute_name(:rmk) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.name %></td>
        <td><%= contact.email %></td>
        <td><%= contact.kind.description %></td>
        <td><%= contact.rmk %></td>
        <td><%= link_to t('buttons.Show'), contact %></td>
        <td><%= link_to t('buttons.Edit'), edit_contact_path(contact) %></td>
        <td><%= link_to t('buttons.Destroy'), contact, method: :delete, data: { confirm: t('buttons.Message') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('buttons.new_contact'), new_contact_path %>
